# skeleton/

干净骨架的分发副本，供复制到其他仓库复用。包含：`AGENTS.md`（治理规则）、`README.md`（仓库说明，分发前按新仓库改写定位段落）、`learning/INDEX.md`（主题索引）、`.gitignore`。

同步纪律：任何治理规则改动，必须与 `skeleton/` 下对应模板在同一提交内更新（见 AGENTS.md「同步义务」）。

分发方式：将本目录内容复制到目标仓库根目录即可，不要求接收方会 git。
